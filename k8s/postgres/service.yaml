apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: raki
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
