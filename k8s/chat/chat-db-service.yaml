apiVersion: v1
kind: Service
metadata:
  name: chat-db
  namespace: raki
  labels:
    app: chat-db
spec:
  type: ClusterIP
  selector:
    app: chat-db
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
