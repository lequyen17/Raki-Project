apiVersion: v1
kind: Service
metadata:
  name: chat-db
  namespace: raki
  labels:
    app: chat-db
    app.kubernetes.io/name: chat-db
    app.kubernetes.io/part-of: raki
spec:
  type: ClusterIP
  selector:
    app: chat-db
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
