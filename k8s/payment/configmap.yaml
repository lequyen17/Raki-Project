apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-config
  namespace: raki
  labels:
    app: payment-service
data:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://payment-db:5432/payment_db"
  SERVER_PORT: "8080"
