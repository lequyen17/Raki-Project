apiVersion: v1
kind: Service
metadata:
  name: payment-db
  namespace: raki
  labels:
    app: payment-db
    app.kubernetes.io/name: payment-db
    app.kubernetes.io/part-of: raki
spec:
  type: ClusterIP
  selector:
    app: payment-db
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
