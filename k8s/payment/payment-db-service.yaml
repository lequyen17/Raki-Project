apiVersion: v1
kind: Service
metadata:
  name: payment-db
  namespace: raki
  labels:
    app: payment-db
spec:
  type: ClusterIP
  selector:
    app: payment-db
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
