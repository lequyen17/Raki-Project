apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: raki
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
